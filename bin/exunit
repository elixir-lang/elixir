#!/bin/sh
SELF=`readlink $0 || echo $0`
SCRIPT_PATH=`dirname $SELF`
ELIXIR_PATH=`cd $SCRIPT_PATH; pwd`
$ELIXIR_PATH/elixir -e "ExUnit.start" -e "Enum.each Code.argv, fn(x) { Enum.each File.wildcard(x), Code.require_file(_) }" -f "ExUnit.run" "$@"
