on: check_suite
name: CI email
jobs:
  sendEmail:
    name: Send email
    runs-on: ubuntu-latest
    steps:
    - run: false
      if: github.event.conclusion != 'neutral' && github.event.conclusion != 'success'
